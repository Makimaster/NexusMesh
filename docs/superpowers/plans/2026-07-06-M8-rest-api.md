# M8 REST API 扩展 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 M7 已落地的三个业务 Service（AgentService / WorkflowService / ExecutionService）以 14 个 RESTful 端点暴露为 HTTP 契约层，完成鉴权守门、请求校验、视图投影、状态码语义四件事，路由体内零业务逻辑。

**Architecture:** API 网关层，`api/ → services/` 单向依赖最外层。每个资源一个 router 文件（agents / workflows / executions），List/Detail 双 Response Schema（类继承），RBAC 通过 `require_roles` 工厂守门，异常由已注册的全局 handler 自动转 HTTP，路由零 try/except。

**Tech Stack:** FastAPI + Pydantic v2（`ConfigDict(from_attributes=True)`）+ SQLAlchemy 2.0 async + pytest（pytest-asyncio auto mode）+ Starlette TestClient。

---

## 已核对的代码事实（实现前必读）

- **API 前缀**：`/api/v1`（`main.py:38` `app.include_router(api_router, prefix=settings.API_V1_PREFIX)`；conftest 已印证 `/api/v1/auth/...`）。
- **全局异常 handler**：`core/exceptions.py:41` `register_exception_handlers` 已在 `main.py:37` 注册。`NexusMeshException` 子类响应体为 `{"detail": <str>}`，状态码取 `exc.status_code`（`NotFoundError`=404，`ValidationError`=422）。路由层**零 try/except**。
- **鉴权装配铁律**：`core/auth.py:71` `require_roles(*roles)` **已返回 `Depends(_check)`**，路由参数直接 `= require_roles(...)`，**不可**再套 `Depends(...)`。`_check`（auth.py:74）返回 `User` 对象，故 create 端点可 `current_user.id`。
- **`get_current_user`**（auth.py:22）返回 `User`；未带 token 时抛 `HTTPException 401`。测试用 `app.dependency_overrides[get_current_user]` 覆盖。
- **Service 签名**（已逐一核对，无幻觉）：
  - `AgentService(db)`：`create_agent(schema, created_by)` / `get_agent_detail(id)` / `list_agents(limit, offset)` / `update_agent(id, schema)` / `delete_agent(id)`
  - `WorkflowService(db, coordinator)`：CRUD 同上 + `trigger_execution(workflow_id, task_input) -> uuid.UUID`
  - `ExecutionService(db)`：`get_execution_detail(id)` / `list_executions(workflow_id, status, limit, offset)` / `get_execution_timeline(id)`
- **已存在 Request Schema**：`AgentRequest`/`AgentUpdateRequest`（schemas/agent.py）、`WorkflowRequest`/`WorkflowUpdateRequest`/`TriggerRequest`（schemas/workflow.py）。**不新建，直接复用**。
- **测试范式**：`TestClient(app)` + `monkeypatch.setattr(ServiceClass, "method", fake_async_fn)` + `app.dependency_overrides` 覆盖鉴权。pytest-asyncio auto mode，测试函数**无需** `@pytest.mark.asyncio`（但 TestClient 测试本身是同步函数）。
- **测试运行**：`cd backend && .venv/Scripts/python.exe -m pytest <路径> -v`

---

## File Structure

| 文件 | 操作 | 责任 |
|------|------|------|
| `backend/app/schemas/execution.py` | 新建 | Execution 四个 Response Schema |
| `backend/app/schemas/agent.py` | 追加 | `AgentListItem` + `AgentResponse` |
| `backend/app/schemas/workflow.py` | 追加 | `WorkflowListItem` + `WorkflowResponse` |
| `backend/app/api/v1/agents.py` | 新建 | Agent 5 端点 |
| `backend/app/api/v1/workflows.py` | 新建 | Workflow 5 CRUD + trigger（6 端点） |
| `backend/app/api/v1/executions.py` | 新建 | Execution 3 只读端点 |
| `backend/app/api/v1/__init__.py` | 修改 | 挂载三个新 router |
| `backend/tests/integration/test_api_m8_schemas.py` | 新建 | Response Schema 投影单测 |
| `backend/tests/integration/test_api_m8_agents.py` | 新建 | Agent 路由 HTTP 测试 |
| `backend/tests/integration/test_api_m8_workflows.py` | 新建 | Workflow 路由 HTTP 测试 |
| `backend/tests/integration/test_api_m8_executions.py` | 新建 | Execution 路由 HTTP 测试 |

---

## Task 1: Response Schemas（视图投影层）

**Files:**
- Create: `backend/app/schemas/execution.py`
- Modify: `backend/app/schemas/agent.py`（追加，现有 1-11 行不动）
- Modify: `backend/app/schemas/workflow.py`（追加，现有 1-17 行不动）
- Test: `backend/tests/integration/test_api_m8_schemas.py`

- [ ] **Step 1: 写失败测试（验证 from_attributes 投影 + 字段裁剪）**

创建 `backend/tests/integration/test_api_m8_schemas.py`：

```python
"""M8 Response Schema 投影单测：验证 from_attributes 投影与字段裁剪。"""

import uuid
from datetime import datetime, timezone
from types import SimpleNamespace

from app.schemas.agent import AgentListItem, AgentResponse
from app.schemas.execution import (
    ExecutionAcceptedResponse,
    ExecutionListItem,
    ExecutionResponse,
    TimelineEventResponse,
)
from app.schemas.workflow import WorkflowListItem, WorkflowResponse


def _now() -> datetime:
    return datetime.now(timezone.utc)


def test_agent_list_item_strips_heavy_fields() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        name="a1",
        agent_type="assistant",
        llm_provider="openai",
        llm_model="gpt-4o",
        system_prompt="secret",
        config={"k": "v"},
        created_at=_now(),
        is_active=True,
    )
    item = AgentListItem.model_validate(orm)
    dumped = item.model_dump()
    assert "system_prompt" not in dumped
    assert "config" not in dumped
    assert "is_active" not in dumped
    assert dumped["llm_model"] == "gpt-4o"


def test_agent_response_includes_heavy_fields() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        name="a1",
        agent_type="assistant",
        llm_provider="openai",
        llm_model="gpt-4o",
        system_prompt="secret",
        config={"k": "v"},
        created_at=_now(),
    )
    resp = AgentResponse.model_validate(orm)
    assert resp.system_prompt == "secret"
    assert resp.config == {"k": "v"}


def test_workflow_list_item_strips_topology() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        name="w1",
        description=None,
        topology={"nodes": [1, 2, 3]},
        created_at=_now(),
    )
    item = WorkflowListItem.model_validate(orm)
    assert "topology" not in item.model_dump()


def test_workflow_response_includes_topology() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        name="w1",
        description="d",
        topology={"nodes": []},
        created_at=_now(),
    )
    resp = WorkflowResponse.model_validate(orm)
    assert resp.topology == {"nodes": []}


def test_execution_response_and_nullable_workflow_id() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        workflow_id=None,
        status="running",
        created_at=_now(),
        started_at=None,
        finished_at=None,
        input={"q": 1},
        output=None,
        error=None,
    )
    resp = ExecutionResponse.model_validate(orm)
    assert resp.workflow_id is None
    assert resp.input == {"q": 1}


def test_execution_accepted_response_only_id() -> None:
    eid = uuid.uuid4()
    resp = ExecutionAcceptedResponse(execution_id=eid)
    assert resp.model_dump() == {"execution_id": eid}


def test_timeline_event_nullable_execution_id() -> None:
    orm = SimpleNamespace(
        id=uuid.uuid4(),
        execution_id=None,
        protocol_stage="INIT",
        event_type="created",
        payload={},
        created_at=_now(),
    )
    ev = TimelineEventResponse.model_validate(orm)
    assert ev.execution_id is None
    assert ev.protocol_stage == "INIT"
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_schemas.py -v`
Expected: FAIL / ImportError（`app.schemas.execution` 不存在，`AgentListItem` 未定义）

- [ ] **Step 3: 创建 `backend/app/schemas/execution.py`**

```python
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict


class ExecutionListItem(BaseModel):
    """历史执行列表项摘要。"""

    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    workflow_id: uuid.UUID | None = None
    status: str
    created_at: datetime
    started_at: datetime | None = None
    finished_at: datetime | None = None


class ExecutionResponse(ExecutionListItem):
    """执行详情页：继承摘要，追加输入/输出/错误。"""

    input: dict
    output: dict | None = None
    error: str | None = None


class ExecutionAcceptedResponse(BaseModel):
    """HTTP 202 专属响应体：只回执行令牌。"""

    execution_id: uuid.UUID


class TimelineEventResponse(BaseModel):
    """执行时间线单条事件（ASC 正序）。"""

    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    execution_id: uuid.UUID | None = None
    protocol_stage: str
    event_type: str
    payload: dict
    created_at: datetime
```

- [ ] **Step 4: 追加 Agent Response Schema 到 `backend/app/schemas/agent.py`**

在文件末尾追加（现有 `AgentRequest`/`AgentUpdateRequest` 不动），并把顶部 import 补齐：

```python
# 顶部 import 追加（现有为 `from pydantic import BaseModel, Field`）
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field


# 文件末尾追加：
class AgentListItem(BaseModel):
    """列表页摘要视图：剥离 system_prompt / config 重字段。"""

    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    name: str
    description: str | None = None
    agent_type: str
    llm_provider: str
    llm_model: str
    created_at: datetime


class AgentResponse(AgentListItem):
    """详情页全量视图：继承摘要，追加重资产字段。"""

    system_prompt: str | None = None
    config: dict
```

- [ ] **Step 5: 追加 Workflow Response Schema 到 `backend/app/schemas/workflow.py`**

在文件末尾追加（现有 `WorkflowRequest`/`WorkflowUpdateRequest`/`TriggerRequest` 不动），补齐顶部 import：

```python
# 顶部 import 追加（现有为 `from pydantic import BaseModel, Field`）
import uuid
from datetime import datetime

from pydantic import BaseModel, ConfigDict, Field


# 文件末尾追加：
class WorkflowListItem(BaseModel):
    """列表页摘要：剥离沉重的 topology JSONB。"""

    model_config = ConfigDict(from_attributes=True)

    id: uuid.UUID
    name: str
    description: str | None = None
    created_at: datetime


class WorkflowResponse(WorkflowListItem):
    """详情页全量：继承摘要，追加核心有向图拓扑。"""

    topology: dict
```

- [ ] **Step 6: 运行测试验证通过**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_schemas.py -v`
Expected: PASS（7 passed）

- [ ] **Step 7: Commit**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/app/schemas/execution.py backend/app/schemas/agent.py backend/app/schemas/workflow.py backend/tests/integration/test_api_m8_schemas.py && git commit -m "feat: M8 Response Schema（List/Detail 双视图 + 字段裁剪）"
```

---

## Task 2: Agent Router（5 端点 + 鉴权测试范式）

> 本任务确立 M8 全部路由测试的鉴权 mock 范式（`app.dependency_overrides` + `monkeypatch`），后续 Task 3/4 复用。

**Files:**
- Create: `backend/app/api/v1/agents.py`
- Test: `backend/tests/integration/test_api_m8_agents.py`

- [ ] **Step 1: 写失败测试（鉴权范式 + 5 端点）**

创建 `backend/tests/integration/test_api_m8_agents.py`：

```python
"""M8 Agent 路由 HTTP 测试：鉴权守门 + 状态码 + 投影。

Service 层以 monkeypatch 打桩，鉴权以 dependency_overrides 覆盖，不连真实 DB。
"""

import uuid
from datetime import datetime, timezone
from types import SimpleNamespace

import pytest
from fastapi.testclient import TestClient

from app.core.auth import get_current_user
from app.main import app
from app.services.agent_service import AgentService

BASE = "/api/v1/agents"


def _agent_orm(**over):
    base = dict(
        id=uuid.uuid4(),
        name="a1",
        agent_type="assistant",
        llm_provider="openai",
        llm_model="gpt-4o",
        system_prompt="sp",
        config={},
        created_at=datetime.now(timezone.utc),
    )
    base.update(over)
    return SimpleNamespace(**base)


@pytest.fixture
def as_developer():
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000001"), role="developer"
    )
    yield
    app.dependency_overrides.clear()


@pytest.fixture
def as_viewer():
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000002"), role="viewer"
    )
    yield
    app.dependency_overrides.clear()


def test_create_agent_unauthenticated_401() -> None:
    # 不覆盖 get_current_user，无 token → 401
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "x", "agent_type": "assistant",
                                   "llm_provider": "openai", "llm_model": "gpt-4o"})
    assert resp.status_code == 401


def test_create_agent_viewer_forbidden_403(as_viewer) -> None:
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "x", "agent_type": "assistant",
                                   "llm_provider": "openai", "llm_model": "gpt-4o"})
    assert resp.status_code == 403


def test_create_agent_developer_201(as_developer, monkeypatch) -> None:
    created = _agent_orm(name="new-agent")

    async def fake_create(self, schema, created_by):
        assert created_by == uuid.UUID("00000000-0000-0000-0000-000000000001")
        return created

    monkeypatch.setattr(AgentService, "create_agent", fake_create)
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "new-agent", "agent_type": "assistant",
                                   "llm_provider": "openai", "llm_model": "gpt-4o"})
    assert resp.status_code == 201
    body = resp.json()
    assert body["name"] == "new-agent"
    assert body["system_prompt"] == "sp"  # detail 视图含重字段


def test_list_agents_viewer_200(as_viewer, monkeypatch) -> None:
    async def fake_list(self, limit=20, offset=0):
        return [_agent_orm(name="a1"), _agent_orm(name="a2")]

    monkeypatch.setattr(AgentService, "list_agents", fake_list)
    client = TestClient(app)
    resp = client.get(BASE)
    assert resp.status_code == 200
    body = resp.json()
    assert len(body) == 2
    assert "system_prompt" not in body[0]  # list 视图剥离重字段


def test_get_agent_detail_200(as_viewer, monkeypatch) -> None:
    target = _agent_orm(name="detail-agent")

    async def fake_detail(self, agent_id):
        return target

    monkeypatch.setattr(AgentService, "get_agent_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{target.id}")
    assert resp.status_code == 200
    assert resp.json()["config"] == {}


def test_get_agent_not_found_404(as_viewer, monkeypatch) -> None:
    from app.core.exceptions import NotFoundError

    async def fake_detail(self, agent_id):
        raise NotFoundError("Agent 不存在或已被删除")

    monkeypatch.setattr(AgentService, "get_agent_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 404
    assert "detail" in resp.json()


def test_update_agent_developer_200(as_developer, monkeypatch) -> None:
    async def fake_update(self, agent_id, schema):
        return _agent_orm(name="renamed")

    monkeypatch.setattr(AgentService, "update_agent", fake_update)
    client = TestClient(app)
    resp = client.patch(f"{BASE}/{uuid.uuid4()}", json={"name": "renamed"})
    assert resp.status_code == 200
    assert resp.json()["name"] == "renamed"


def test_update_agent_viewer_forbidden_403(as_viewer) -> None:
    client = TestClient(app)
    resp = client.patch(f"{BASE}/{uuid.uuid4()}", json={"name": "x"})
    assert resp.status_code == 403


def test_delete_agent_developer_204(as_developer, monkeypatch) -> None:
    async def fake_delete(self, agent_id):
        return None

    monkeypatch.setattr(AgentService, "delete_agent", fake_delete)
    client = TestClient(app)
    resp = client.delete(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 204


def test_delete_agent_viewer_forbidden_403(as_viewer) -> None:
    client = TestClient(app)
    resp = client.delete(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 403
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_agents.py -v`
Expected: FAIL — 全部 404（`/api/v1/agents` 路由未注册，因 `agents.py` 不存在且未挂载）

- [ ] **Step 3: 创建 `backend/app/api/v1/agents.py`**

```python
import uuid

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.auth import UserRole, get_current_user, require_roles
from app.db.session import get_db
from app.models.user import User
from app.schemas.agent import (
    AgentListItem,
    AgentRequest,
    AgentResponse,
    AgentUpdateRequest,
)
from app.services.agent_service import AgentService

router = APIRouter(prefix="/agents", tags=["agents"])


@router.post("", response_model=AgentResponse, status_code=201)
async def create_agent(
    payload: AgentRequest,
    db: AsyncSession = Depends(get_db),
    current_user: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> AgentResponse:
    agent = await AgentService(db).create_agent(payload, created_by=current_user.id)
    return AgentResponse.model_validate(agent)


@router.get("", response_model=list[AgentListItem])
async def list_agents(
    limit: int = Query(20, ge=1, le=100),
    offset: int = Query(0, ge=0),
    db: AsyncSession = Depends(get_db),
    _: User = Depends(get_current_user),
) -> list[AgentListItem]:
    agents = await AgentService(db).list_agents(limit=limit, offset=offset)
    return [AgentListItem.model_validate(a) for a in agents]


@router.get("/{agent_id}", response_model=AgentResponse)
async def get_agent(
    agent_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    _: User = Depends(get_current_user),
) -> AgentResponse:
    agent = await AgentService(db).get_agent_detail(agent_id)
    return AgentResponse.model_validate(agent)


@router.patch("/{agent_id}", response_model=AgentResponse)
async def update_agent(
    agent_id: uuid.UUID,
    payload: AgentUpdateRequest,
    db: AsyncSession = Depends(get_db),
    _: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> AgentResponse:
    agent = await AgentService(db).update_agent(agent_id, payload)
    return AgentResponse.model_validate(agent)


@router.delete("/{agent_id}", status_code=204)
async def delete_agent(
    agent_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    _: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> None:
    await AgentService(db).delete_agent(agent_id)
```

- [ ] **Step 4: 临时挂载 router（让测试可命中路由）**

修改 `backend/app/api/v1/__init__.py`，追加 agents_router（Task 5 会补齐其余两个）：

```python
from fastapi import APIRouter

from app.api.v1.agents import router as agents_router
from app.api.v1.auth import router as auth_router
from app.api.v1.health import router as health_router

api_router = APIRouter()
api_router.include_router(auth_router)
api_router.include_router(health_router)
api_router.include_router(agents_router)
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_agents.py -v`
Expected: PASS（10 passed）

- [ ] **Step 6: Commit**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/app/api/v1/agents.py backend/app/api/v1/__init__.py backend/tests/integration/test_api_m8_agents.py && git commit -m "feat: M8 Agent 路由（5 端点 + RBAC 守门 + 双视图投影）"
```

---

## Task 3: Workflow Router（5 CRUD + trigger 端点）

> **关键差异**：trigger 端点依赖 `get_coordinator`（内部 `Depends(get_redis)`，会连 Redis）。FastAPI 在进入路由体前就解析该依赖，故测试必须用 `app.dependency_overrides[get_coordinator]` 覆盖为哑对象——即便 `trigger_execution` 已被 monkeypatch，依赖解析仍会先执行。

**Files:**
- Create: `backend/app/api/v1/workflows.py`
- Test: `backend/tests/integration/test_api_m8_workflows.py`

- [ ] **Step 1: 写失败测试（5 CRUD + trigger 202/403/404）**

创建 `backend/tests/integration/test_api_m8_workflows.py`：

```python
"""M8 Workflow 路由 HTTP 测试：CRUD + 异步 trigger（202 Accepted）。"""

import uuid
from datetime import datetime, timezone
from types import SimpleNamespace

import pytest
from fastapi.testclient import TestClient

from app.api.dependencies.orchestrator import get_coordinator
from app.core.auth import get_current_user
from app.main import app
from app.services.workflow_service import WorkflowService

BASE = "/api/v1/workflows"


def _wf_orm(**over):
    base = dict(
        id=uuid.uuid4(),
        name="w1",
        description="d",
        topology={"nodes": [], "edges": []},
        created_at=datetime.now(timezone.utc),
    )
    base.update(over)
    return SimpleNamespace(**base)


@pytest.fixture
def as_developer():
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000001"), role="developer"
    )
    yield
    app.dependency_overrides.clear()


@pytest.fixture
def as_viewer():
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000002"), role="viewer"
    )
    yield
    app.dependency_overrides.clear()


@pytest.fixture
def as_developer_with_coordinator():
    """trigger 端点专用：同时覆盖鉴权与 coordinator（避免连 Redis）。"""
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000001"), role="developer"
    )
    app.dependency_overrides[get_coordinator] = lambda: object()
    yield
    app.dependency_overrides.clear()


def test_create_workflow_developer_201(as_developer, monkeypatch) -> None:
    async def fake_create(self, schema, created_by):
        assert created_by == uuid.UUID("00000000-0000-0000-0000-000000000001")
        return _wf_orm(name="new-wf")

    monkeypatch.setattr(WorkflowService, "create_workflow", fake_create)
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "new-wf", "topology": {"nodes": [], "edges": []}})
    assert resp.status_code == 201
    assert resp.json()["topology"] == {"nodes": [], "edges": []}


def test_create_workflow_invalid_topology_422(as_developer, monkeypatch) -> None:
    from app.core.exceptions import ValidationError

    async def fake_create(self, schema, created_by):
        raise ValidationError("拓扑非法：画布节点集合为空")

    monkeypatch.setattr(WorkflowService, "create_workflow", fake_create)
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "bad", "topology": {}})
    assert resp.status_code == 422


def test_create_workflow_viewer_forbidden_403(as_viewer) -> None:
    client = TestClient(app)
    resp = client.post(BASE, json={"name": "x", "topology": {}})
    assert resp.status_code == 403


def test_list_workflows_200(as_viewer, monkeypatch) -> None:
    async def fake_list(self, limit=20, offset=0):
        return [_wf_orm(), _wf_orm()]

    monkeypatch.setattr(WorkflowService, "list_workflows", fake_list)
    client = TestClient(app)
    resp = client.get(BASE)
    assert resp.status_code == 200
    body = resp.json()
    assert len(body) == 2
    assert "topology" not in body[0]  # list 视图剥离 topology


def test_get_workflow_detail_200(as_viewer, monkeypatch) -> None:
    target = _wf_orm()

    async def fake_detail(self, workflow_id):
        return target

    monkeypatch.setattr(WorkflowService, "get_workflow_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{target.id}")
    assert resp.status_code == 200
    assert "topology" in resp.json()  # detail 视图含 topology


def test_get_workflow_not_found_404(as_viewer, monkeypatch) -> None:
    from app.core.exceptions import NotFoundError

    async def fake_detail(self, workflow_id):
        raise NotFoundError("工作流不存在或已被删除")

    monkeypatch.setattr(WorkflowService, "get_workflow_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 404


def test_update_workflow_developer_200(as_developer, monkeypatch) -> None:
    async def fake_update(self, workflow_id, schema):
        return _wf_orm(name="renamed")

    monkeypatch.setattr(WorkflowService, "update_workflow", fake_update)
    client = TestClient(app)
    resp = client.patch(f"{BASE}/{uuid.uuid4()}", json={"name": "renamed"})
    assert resp.status_code == 200
    assert resp.json()["name"] == "renamed"


def test_delete_workflow_developer_204(as_developer, monkeypatch) -> None:
    async def fake_delete(self, workflow_id):
        return None

    monkeypatch.setattr(WorkflowService, "delete_workflow", fake_delete)
    client = TestClient(app)
    resp = client.delete(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 204


def test_trigger_execution_developer_202(as_developer_with_coordinator, monkeypatch) -> None:
    new_eid = uuid.uuid4()

    async def fake_trigger(self, workflow_id, task_input):
        assert task_input == {"q": "hello"}
        return new_eid

    monkeypatch.setattr(WorkflowService, "trigger_execution", fake_trigger)
    client = TestClient(app)
    resp = client.post(f"{BASE}/{uuid.uuid4()}/execute", json={"task_input": {"q": "hello"}})
    assert resp.status_code == 202
    assert resp.json() == {"execution_id": str(new_eid)}


def test_trigger_execution_viewer_forbidden_403(as_viewer) -> None:
    client = TestClient(app)
    resp = client.post(f"{BASE}/{uuid.uuid4()}/execute", json={"task_input": {}})
    assert resp.status_code == 403


def test_trigger_execution_workflow_not_found_404(
    as_developer_with_coordinator, monkeypatch
) -> None:
    from app.core.exceptions import NotFoundError

    async def fake_trigger(self, workflow_id, task_input):
        raise NotFoundError("工作流不存在或已被删除")

    monkeypatch.setattr(WorkflowService, "trigger_execution", fake_trigger)
    client = TestClient(app)
    resp = client.post(f"{BASE}/{uuid.uuid4()}/execute", json={"task_input": {}})
    assert resp.status_code == 404
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_workflows.py -v`
Expected: FAIL — 全部 404（`workflows.py` 不存在且未挂载）

- [ ] **Step 3: 创建 `backend/app/api/v1/workflows.py`**

```python
import uuid

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.api.dependencies.orchestrator import get_coordinator
from app.core.auth import UserRole, get_current_user, require_roles
from app.db.session import get_db
from app.models.user import User
from app.orchestrator.coordinator import Coordinator
from app.schemas.execution import ExecutionAcceptedResponse
from app.schemas.workflow import (
    TriggerRequest,
    WorkflowListItem,
    WorkflowRequest,
    WorkflowResponse,
    WorkflowUpdateRequest,
)
from app.services.workflow_service import WorkflowService

router = APIRouter(prefix="/workflows", tags=["workflows"])


@router.post("", response_model=WorkflowResponse, status_code=201)
async def create_workflow(
    payload: WorkflowRequest,
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    current_user: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> WorkflowResponse:
    service = WorkflowService(db=db, coordinator=coordinator)
    workflow = await service.create_workflow(payload, created_by=current_user.id)
    return WorkflowResponse.model_validate(workflow)


@router.get("", response_model=list[WorkflowListItem])
async def list_workflows(
    limit: int = Query(20, ge=1, le=100),
    offset: int = Query(0, ge=0),
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    _: User = Depends(get_current_user),
) -> list[WorkflowListItem]:
    service = WorkflowService(db=db, coordinator=coordinator)
    workflows = await service.list_workflows(limit=limit, offset=offset)
    return [WorkflowListItem.model_validate(w) for w in workflows]


@router.get("/{workflow_id}", response_model=WorkflowResponse)
async def get_workflow(
    workflow_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    _: User = Depends(get_current_user),
) -> WorkflowResponse:
    service = WorkflowService(db=db, coordinator=coordinator)
    workflow = await service.get_workflow_detail(workflow_id)
    return WorkflowResponse.model_validate(workflow)


@router.patch("/{workflow_id}", response_model=WorkflowResponse)
async def update_workflow(
    workflow_id: uuid.UUID,
    payload: WorkflowUpdateRequest,
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    _: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> WorkflowResponse:
    service = WorkflowService(db=db, coordinator=coordinator)
    workflow = await service.update_workflow(workflow_id, payload)
    return WorkflowResponse.model_validate(workflow)


@router.delete("/{workflow_id}", status_code=204)
async def delete_workflow(
    workflow_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    _: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> None:
    service = WorkflowService(db=db, coordinator=coordinator)
    await service.delete_workflow(workflow_id)


@router.post(
    "/{workflow_id}/execute",
    response_model=ExecutionAcceptedResponse,
    status_code=202,
)
async def trigger_workflow(
    workflow_id: uuid.UUID,
    payload: TriggerRequest,
    db: AsyncSession = Depends(get_db),
    coordinator: Coordinator = Depends(get_coordinator),
    _: User = require_roles(UserRole.DEVELOPER, UserRole.ADMIN),
) -> ExecutionAcceptedResponse:
    service = WorkflowService(db=db, coordinator=coordinator)
    execution_id = await service.trigger_execution(
        workflow_id=workflow_id, task_input=payload.task_input
    )
    return ExecutionAcceptedResponse(execution_id=execution_id)
```

> **设计校准（CRUD 端点为何也注入 coordinator）**：`WorkflowService.__init__(db, coordinator)` 是必填双参构造。即便 CRUD 不触发执行，也须传入 coordinator 满足构造契约。`get_coordinator` 是进程内单例（幂等），注入成本可忽略。这与 M7 `WorkflowService(db, coordinator)` 签名严格对齐。

- [ ] **Step 4: 临时挂载 workflows_router 到 `__init__.py`**

在 Task 2 已改的 `backend/app/api/v1/__init__.py` 基础上追加 workflows：

```python
from fastapi import APIRouter

from app.api.v1.agents import router as agents_router
from app.api.v1.auth import router as auth_router
from app.api.v1.health import router as health_router
from app.api.v1.workflows import router as workflows_router

api_router = APIRouter()
api_router.include_router(auth_router)
api_router.include_router(health_router)
api_router.include_router(agents_router)
api_router.include_router(workflows_router)
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_workflows.py -v`
Expected: PASS（11 passed）

- [ ] **Step 6: Commit**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/app/api/v1/workflows.py backend/app/api/v1/__init__.py backend/tests/integration/test_api_m8_workflows.py && git commit -m "feat: M8 Workflow 路由（5 CRUD + 202 异步 trigger）"
```

---

## Task 4: Execution Router（3 只读端点）

**Files:**
- Create: `backend/app/api/v1/executions.py`
- Test: `backend/tests/integration/test_api_m8_executions.py`

- [ ] **Step 1: 写失败测试（list 含过滤 + detail + timeline）**

创建 `backend/tests/integration/test_api_m8_executions.py`：

```python
"""M8 Execution 路由 HTTP 测试：只读查询（list/detail/timeline）。"""

import uuid
from datetime import datetime, timezone
from types import SimpleNamespace

import pytest
from fastapi.testclient import TestClient

from app.core.auth import get_current_user
from app.main import app
from app.services.execution_service import ExecutionService

BASE = "/api/v1/executions"


def _exec_orm(**over):
    base = dict(
        id=uuid.uuid4(),
        workflow_id=uuid.uuid4(),
        status="running",
        created_at=datetime.now(timezone.utc),
        started_at=None,
        finished_at=None,
        input={"q": 1},
        output=None,
        error=None,
    )
    base.update(over)
    return SimpleNamespace(**base)


def _event_orm(**over):
    base = dict(
        id=uuid.uuid4(),
        execution_id=uuid.uuid4(),
        protocol_stage="INIT",
        event_type="created",
        payload={},
        created_at=datetime.now(timezone.utc),
    )
    base.update(over)
    return SimpleNamespace(**base)


@pytest.fixture
def as_viewer():
    app.dependency_overrides[get_current_user] = lambda: SimpleNamespace(
        id=uuid.UUID("00000000-0000-0000-0000-000000000002"), role="viewer"
    )
    yield
    app.dependency_overrides.clear()


def test_list_executions_unauthenticated_401() -> None:
    client = TestClient(app)
    resp = client.get(BASE)
    assert resp.status_code == 401


def test_list_executions_200_strips_heavy_fields(as_viewer, monkeypatch) -> None:
    async def fake_list(self, workflow_id=None, status=None, limit=20, offset=0):
        return [_exec_orm(), _exec_orm()]

    monkeypatch.setattr(ExecutionService, "list_executions", fake_list)
    client = TestClient(app)
    resp = client.get(BASE)
    assert resp.status_code == 200
    body = resp.json()
    assert len(body) == 2
    assert "input" not in body[0]  # list 视图剥离 input/output/error


def test_list_executions_passes_filters(as_viewer, monkeypatch) -> None:
    captured = {}

    async def fake_list(self, workflow_id=None, status=None, limit=20, offset=0):
        captured.update(
            workflow_id=workflow_id, status=status, limit=limit, offset=offset
        )
        return []

    monkeypatch.setattr(ExecutionService, "list_executions", fake_list)
    wf_id = uuid.uuid4()
    client = TestClient(app)
    resp = client.get(f"{BASE}?workflow_id={wf_id}&status=running&limit=5&offset=10")
    assert resp.status_code == 200
    assert captured["workflow_id"] == wf_id
    assert captured["status"] == "running"
    assert captured["limit"] == 5
    assert captured["offset"] == 10


def test_get_execution_detail_200(as_viewer, monkeypatch) -> None:
    target = _exec_orm(status="succeeded", output={"result": 42})

    async def fake_detail(self, execution_id):
        return target

    monkeypatch.setattr(ExecutionService, "get_execution_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{target.id}")
    assert resp.status_code == 200
    body = resp.json()
    assert body["output"] == {"result": 42}  # detail 视图含 output


def test_get_execution_not_found_404(as_viewer, monkeypatch) -> None:
    from app.core.exceptions import NotFoundError

    async def fake_detail(self, execution_id):
        raise NotFoundError("执行实例不存在")

    monkeypatch.setattr(ExecutionService, "get_execution_detail", fake_detail)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{uuid.uuid4()}")
    assert resp.status_code == 404


def test_get_execution_timeline_200(as_viewer, monkeypatch) -> None:
    async def fake_timeline(self, execution_id):
        return [_event_orm(protocol_stage="INIT"), _event_orm(protocol_stage="RECEIVE")]

    monkeypatch.setattr(ExecutionService, "get_execution_timeline", fake_timeline)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{uuid.uuid4()}/timeline")
    assert resp.status_code == 200
    body = resp.json()
    assert len(body) == 2
    assert body[0]["protocol_stage"] == "INIT"


def test_get_timeline_execution_not_found_404(as_viewer, monkeypatch) -> None:
    from app.core.exceptions import NotFoundError

    async def fake_timeline(self, execution_id):
        raise NotFoundError("执行实例不存在")

    monkeypatch.setattr(ExecutionService, "get_execution_timeline", fake_timeline)
    client = TestClient(app)
    resp = client.get(f"{BASE}/{uuid.uuid4()}/timeline")
    assert resp.status_code == 404
```

- [ ] **Step 2: 运行测试验证失败**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_executions.py -v`
Expected: FAIL — 全部 404（`executions.py` 不存在且未挂载）

- [ ] **Step 3: 创建 `backend/app/api/v1/executions.py`**

```python
import uuid

from fastapi import APIRouter, Depends, Query
from sqlalchemy.ext.asyncio import AsyncSession

from app.core.auth import get_current_user
from app.db.session import get_db
from app.models.user import User
from app.schemas.execution import (
    ExecutionListItem,
    ExecutionResponse,
    TimelineEventResponse,
)
from app.services.execution_service import ExecutionService

router = APIRouter(prefix="/executions", tags=["executions"])


@router.get("", response_model=list[ExecutionListItem])
async def list_executions(
    workflow_id: uuid.UUID | None = Query(None),
    status: str | None = Query(None),
    limit: int = Query(20, ge=1, le=100),
    offset: int = Query(0, ge=0),
    db: AsyncSession = Depends(get_db),
    _: User = Depends(get_current_user),
) -> list[ExecutionListItem]:
    executions = await ExecutionService(db).list_executions(
        workflow_id=workflow_id, status=status, limit=limit, offset=offset
    )
    return [ExecutionListItem.model_validate(e) for e in executions]


@router.get("/{execution_id}", response_model=ExecutionResponse)
async def get_execution(
    execution_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    _: User = Depends(get_current_user),
) -> ExecutionResponse:
    execution = await ExecutionService(db).get_execution_detail(execution_id)
    return ExecutionResponse.model_validate(execution)


@router.get("/{execution_id}/timeline", response_model=list[TimelineEventResponse])
async def get_execution_timeline(
    execution_id: uuid.UUID,
    db: AsyncSession = Depends(get_db),
    _: User = Depends(get_current_user),
) -> list[TimelineEventResponse]:
    events = await ExecutionService(db).get_execution_timeline(execution_id)
    return [TimelineEventResponse.model_validate(e) for e in events]
```

- [ ] **Step 4: 临时挂载 executions_router 到 `__init__.py`**（Task 5 会做最终校验）

```python
from app.api.v1.executions import router as executions_router
# ...
api_router.include_router(executions_router)
```

- [ ] **Step 5: 运行测试验证通过**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_executions.py -v`
Expected: PASS（7 passed）

- [ ] **Step 6: Commit**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/app/api/v1/executions.py backend/app/api/v1/__init__.py backend/tests/integration/test_api_m8_executions.py && git commit -m "feat: M8 Execution 路由（list/detail/timeline 只读查询）"
```

---

## Task 5: Router 挂载收口与全量注册校验

> 前三个任务已逐步追加 router。本任务确认 `__init__.py` 最终态完整，并写一个"路由注册齐全"的守卫测试，防止未来漏挂。

**Files:**
- Modify: `backend/app/api/v1/__init__.py`（确认最终态）
- Test: `backend/tests/integration/test_api_m8_routing.py`

- [ ] **Step 1: 写路由注册守卫测试**

创建 `backend/tests/integration/test_api_m8_routing.py`：

```python
"""M8 路由注册守卫：确保 14 个端点全部挂载到 app。"""

from app.main import app

EXPECTED_PATHS = {
    ("/api/v1/agents", "POST"),
    ("/api/v1/agents", "GET"),
    ("/api/v1/agents/{agent_id}", "GET"),
    ("/api/v1/agents/{agent_id}", "PATCH"),
    ("/api/v1/agents/{agent_id}", "DELETE"),
    ("/api/v1/workflows", "POST"),
    ("/api/v1/workflows", "GET"),
    ("/api/v1/workflows/{workflow_id}", "GET"),
    ("/api/v1/workflows/{workflow_id}", "PATCH"),
    ("/api/v1/workflows/{workflow_id}", "DELETE"),
    ("/api/v1/workflows/{workflow_id}/execute", "POST"),
    ("/api/v1/executions", "GET"),
    ("/api/v1/executions/{execution_id}", "GET"),
    ("/api/v1/executions/{execution_id}/timeline", "GET"),
}


def test_all_m8_routes_registered() -> None:
    registered = set()
    for route in app.routes:
        methods = getattr(route, "methods", None)
        path = getattr(route, "path", None)
        if methods and path:
            for method in methods:
                registered.add((path, method))
    missing = EXPECTED_PATHS - registered
    assert not missing, f"未注册的 M8 端点: {missing}"
```

- [ ] **Step 2: 运行测试**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_routing.py -v`
Expected: PASS（若前三个任务的临时挂载都正确，直接通过）

- [ ] **Step 3: 确认 `backend/app/api/v1/__init__.py` 最终态**

确认内容与下方完全一致（import 按字母序，Ruff I 规则友好）：

```python
from fastapi import APIRouter

from app.api.v1.agents import router as agents_router
from app.api.v1.auth import router as auth_router
from app.api.v1.executions import router as executions_router
from app.api.v1.health import router as health_router
from app.api.v1.workflows import router as workflows_router

api_router = APIRouter()
api_router.include_router(auth_router)
api_router.include_router(health_router)
api_router.include_router(agents_router)
api_router.include_router(workflows_router)
api_router.include_router(executions_router)
```

- [ ] **Step 4: 重跑守卫测试确认仍通过**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_routing.py -v`
Expected: PASS（1 passed）

- [ ] **Step 5: Commit**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/app/api/v1/__init__.py backend/tests/integration/test_api_m8_routing.py && git commit -m "test: M8 路由注册守卫（14 端点齐全性校验）"
```

---

## Task 6: Ruff 静态检查 + 全量回归

> 全部功能落地后，跑 Ruff 与完整测试套件，确保 M8 无 lint 违规、无回归。

**Files:** 无新增，可能按 Ruff 反馈微调 imports。

- [ ] **Step 1: 运行 Ruff 检查 M8 新增/修改文件**

Run:
```bash
cd backend && .venv/Scripts/python.exe -m ruff check app/schemas/execution.py app/schemas/agent.py app/schemas/workflow.py app/api/v1/agents.py app/api/v1/workflows.py app/api/v1/executions.py app/api/v1/__init__.py tests/integration/test_api_m8_schemas.py tests/integration/test_api_m8_agents.py tests/integration/test_api_m8_workflows.py tests/integration/test_api_m8_executions.py tests/integration/test_api_m8_routing.py
```
Expected: `All checks passed!`

- [ ] **Step 2: 若有 Ruff 违规则修复**

常见项：import 排序（I 规则）、未使用 import（F401）。按提示修复。若无违规跳过此步。

Run（自动修复安全项）: `cd backend && .venv/Scripts/python.exe -m ruff check --fix <违规文件>`

- [ ] **Step 3: 运行 M8 全部测试**

Run: `cd backend && .venv/Scripts/python.exe -m pytest tests/integration/test_api_m8_schemas.py tests/integration/test_api_m8_agents.py tests/integration/test_api_m8_workflows.py tests/integration/test_api_m8_executions.py tests/integration/test_api_m8_routing.py -v`
Expected: PASS（8 + 18 + 26 + 12 + 1 = 65 passed）

- [ ] **Step 4: 运行完整测试套件（确认无回归）**

Run: `cd backend && .venv/Scripts/python.exe -m pytest -v`
Expected: 全绿或仅 PG/Redis 未就绪相关的既有 skip（M8 HTTP 测试全 mock，不受 PG/Redis 影响，必须全 PASS）

- [ ] **Step 5: Commit（若 Step 2 有修复）**

```bash
cd /c/gamemaster/Python/Pycharm/NexusMesh && git add backend/ && git commit -m "style: M8 Ruff 校准与全量回归通过"
```

若 Step 2 无任何修改，跳过本步（无需空提交）。

---

## 自审记录（写计划时已核对）

**1. Spec 覆盖检查（逐节对照 design 文档）：**

| Spec 章节 | 对应任务 | 覆盖 |
|-----------|---------|------|
| §3.1 Agent 5 端点 | Task 2 | ✅ |
| §3.2 Workflow 5 端点 | Task 3 | ✅ |
| §3.3 trigger 202 + Execution 3 端点 | Task 3（trigger）+ Task 4 | ✅ |
| §4.1–4.4 四组 Response Schema | Task 1 | ✅ |
| §4.5 字段裁剪（is_active/updated_at/org_id/created_by 不暴露） | Task 1 测试断言 `not in dumped` | ✅ |
| §6.1 鉴权三模式（A读/B丢弃/C取id） | Task 2 建立范式，403/401 测试 | ✅ |
| §6.3 trigger 用 TriggerRequest + 202 | Task 3 Step 3 | ✅ |
| §7 Router 挂载 | Task 5 | ✅ |
| §8 全局异常零 try/except（404/422） | Task 2/3/4 的 404/422 测试 | ✅ |

**2. Placeholder 扫描：** 无 TBD/TODO；每个 code step 均含完整代码；无"类似 Task N"引用（trigger/CRUD 代码全量写出）。

**3. 类型/签名一致性核对：**
- `AgentListItem`/`AgentResponse`/`WorkflowListItem`/`WorkflowResponse`/`ExecutionListItem`/`ExecutionResponse`/`ExecutionAcceptedResponse`/`TimelineEventResponse` — Task 1 定义，Task 2/3/4 消费，命名一致。
- Service 方法名（`create_agent`/`list_agents`/`get_agent_detail`/`update_agent`/`delete_agent`/`create_workflow`/`list_workflows`/`get_workflow_detail`/`update_workflow`/`delete_workflow`/`trigger_execution`/`list_executions`/`get_execution_detail`/`get_execution_timeline`）— 全部核对真实 Service 源码，零幻觉。
- `require_roles(...)` 直接赋值不套 `Depends`；`get_current_user` 用 `Depends()` 包裹 — 与 auth.py 契约一致。
- trigger 端点路径参数命名 `workflow_id`，与守卫测试 `EXPECTED_PATHS` 中 `{workflow_id}` 一致。

**4. 已知实现注意点（给执行者）：**
- Workflow **所有** CRUD 端点都注入 `get_coordinator`（构造 `WorkflowService(db, coordinator)` 必填），测试须覆盖 `get_coordinator` 依赖（见 Task 3 `as_developer_with_coordinator` fixture）。
- 测试用 `SimpleNamespace` 伪造 ORM 对象喂给 `model_validate`（`from_attributes=True` 按属性读取，SimpleNamespace 属性访问天然兼容）。
- FastAPI 依赖在进入路由体前解析，故未 mock 的 `get_coordinator` 会真连 Redis 导致测试挂起/失败——务必在 trigger/CRUD workflow 测试里覆盖它。
